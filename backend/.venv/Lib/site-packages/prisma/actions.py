# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class LLMSettingsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LLMSettings]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LLMSettings.prisma().query_raw(
            'SELECT * FROM LLMSettings WHERE id = ?',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LLMSettings
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LLMSettings.prisma().query_first(
            'SELECT * FROM LLMSettings WHERE ip = ?',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LLMSettingsCreateInput,
        include: Optional[types.LLMSettingsInclude] = None
    ) -> _PrismaModelT:
        """Create a new LLMSettings record.

        Parameters
        ----------
        data
            LLMSettings record data
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The created LLMSettings record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LLMSettings record from just the required fields
        llmsettings = await LLMSettings.prisma().create(
            data={
                # data to create a LLMSettings record
                'ip': 'ggciceaie',
                'port': 1147902781,
                'modelName': 'cadfabfehe',
                'temperature': 368800899.150802,
                'maxTokens': 486256185,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LLMSettingsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LLMSettings records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LLMSettings record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LLMSettings.prisma().create_many(
            data=[
                {
                    # data to create a LLMSettings record
                    'ip': 'bagcfbhiig',
                    'port': 267834847,
                    'modelName': 'biabhbdai',
                    'temperature': 836760821.59533,
                    'maxTokens': 790425851,
                },
                {
                    # data to create a LLMSettings record
                    'ip': 'cbbbjbfcii',
                    'port': 1149758321,
                    'modelName': 'bgeecijdgg',
                    'temperature': 1388290519.164741,
                    'maxTokens': 1675546029,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LLMSettingsWhereUniqueInput,
        include: Optional[types.LLMSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LLMSettings record.

        Parameters
        ----------
        where
            LLMSettings filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The deleted LLMSettings record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        llmsettings = await LLMSettings.prisma().delete(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LLMSettingsWhereUniqueInput,
        include: Optional[types.LLMSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LLMSettings record.

        Parameters
        ----------
        where
            LLMSettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The found LLMSettings record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        llmsettings = await LLMSettings.prisma().find_unique(
            where={
                'id': 'dcgchcbbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LLMSettingsWhereUniqueInput,
        include: Optional[types.LLMSettingsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LLMSettings record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LLMSettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The found LLMSettings record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        llmsettings = await LLMSettings.prisma().find_unique_or_raise(
            where={
                'id': 'bdedcabahc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
        include: Optional[types.LLMSettingsInclude] = None,
        order: Optional[Union[types.LLMSettingsOrderByInput, List[types.LLMSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.LLMSettingsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LLMSettings records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LLMSettings records returned
        skip
            Ignore the first N results
        where
            LLMSettings filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LLMSettings model
        order
            Order the returned LLMSettings records by any field
        distinct
            Filter LLMSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LLMSettings]
            The list of all LLMSettings records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LLMSettings records
        llmsettings = await LLMSettings.prisma().find_many(take=10)

        # find the first 5 LLMSettings records ordered by the port field
        llmsettings = await LLMSettings.prisma().find_many(
            take=5,
            order={
                'port': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
        include: Optional[types.LLMSettingsInclude] = None,
        order: Optional[Union[types.LLMSettingsOrderByInput, List[types.LLMSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.LLMSettingsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LLMSettings record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LLMSettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LLMSettings model
        order
            Order the returned LLMSettings records by any field
        distinct
            Filter LLMSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LLMSettings
            The first LLMSettings record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LLMSettings record ordered by the modelName field
        llmsettings = await LLMSettings.prisma().find_first(
            skip=1,
            order={
                'modelName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
        include: Optional[types.LLMSettingsInclude] = None,
        order: Optional[Union[types.LLMSettingsOrderByInput, List[types.LLMSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.LLMSettingsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LLMSettings record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LLMSettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LLMSettings model
        order
            Order the returned LLMSettings records by any field
        distinct
            Filter LLMSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LLMSettings
            The first LLMSettings record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LLMSettings record ordered by the temperature field
        llmsettings = await LLMSettings.prisma().find_first_or_raise(
            skip=1,
            order={
                'temperature': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LLMSettingsUpdateInput,
        where: types.LLMSettingsWhereUniqueInput,
        include: Optional[types.LLMSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LLMSettings record.

        Parameters
        ----------
        data
            LLMSettings record data specifying what to update
        where
            LLMSettings filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The updated LLMSettings record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        llmsettings = await LLMSettings.prisma().update(
            where={
                'id': 'ghfhiafcb',
            },
            data={
                # data to update the LLMSettings record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LLMSettingsWhereUniqueInput,
        data: types.LLMSettingsUpsertInput,
        include: Optional[types.LLMSettingsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LLMSettings filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LLMSettings model

        Returns
        -------
        prisma.models.LLMSettings
            The created or updated LLMSettings record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        llmsettings = await LLMSettings.prisma().upsert(
            where={
                'id': 'heejgedji',
            },
            data={
                'create': {
                    'id': 'heejgedji',
                    'ip': 'cbbbjbfcii',
                    'port': 1149758321,
                    'modelName': 'bgeecijdgg',
                    'temperature': 1388290519.164741,
                    'maxTokens': 1675546029,
                },
                'update': {
                    'ip': 'cbbbjbfcii',
                    'port': 1149758321,
                    'modelName': 'bgeecijdgg',
                    'temperature': 1388290519.164741,
                    'maxTokens': 1675546029,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LLMSettingsUpdateManyMutationInput,
        where: types.LLMSettingsWhereInput,
    ) -> int:
        """Update multiple LLMSettings records

        Parameters
        ----------
        data
            LLMSettings data to update the selected LLMSettings records to
        where
            Filter to select the LLMSettings records to update

        Returns
        -------
        int
            The total number of LLMSettings records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LLMSettings records
        total = await LLMSettings.prisma().update_many(
            data={
                'maxTokens': 1969681615
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LLMSettings records present in the database

        Parameters
        ----------
        select
            Select the LLMSettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LLMSettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LLMSettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LLMSettings.prisma().count()

        # results: prisma.types.LLMSettingsCountAggregateOutput
        results = await LLMSettings.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LLMSettingsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
    ) -> types.LLMSettingsCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LLMSettingsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LLMSettingsWhereInput] = None,
        cursor: Optional[types.LLMSettingsWhereUniqueInput] = None,
    ) -> Union[int, types.LLMSettingsCountAggregateOutput]:
        """Count the number of LLMSettings records present in the database

        Parameters
        ----------
        select
            Select the LLMSettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LLMSettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LLMSettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LLMSettings.prisma().count()

        # results: prisma.types.LLMSettingsCountAggregateOutput
        results = await LLMSettings.prisma().count(
            select={
                '_all': True,
                'ip': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LLMSettingsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LLMSettingsWhereInput] = None
    ) -> int:
        """Delete multiple LLMSettings records.

        Parameters
        ----------
        where
            Optional LLMSettings filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LLMSettings records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LLMSettings records
        total = await LLMSettings.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LLMSettingsScalarFieldKeys'],
        *,
        where: Optional['types.LLMSettingsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LLMSettingsAvgAggregateInput'] = None,
        sum: Optional['types.LLMSettingsSumAggregateInput'] = None,
        min: Optional['types.LLMSettingsMinAggregateInput'] = None,
        max: Optional['types.LLMSettingsMaxAggregateInput'] = None,
        having: Optional['types.LLMSettingsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LLMSettingsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LLMSettingsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LLMSettingsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LLMSettingsGroupByOutput']:
        """Group LLMSettings records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LLMSettings fields to group records by
        where
            LLMSettings filter to select records
        take
            Limit the maximum number of LLMSettings records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LLMSettingsGroupByOutput]
            A list of dictionaries representing the LLMSettings record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LLMSettings records by port values
        # and count how many records are in each group
        results = await LLMSettings.prisma().group_by(
            ['port'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BackendSettingsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BackendSettings]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await BackendSettings.prisma().query_raw(
            'SELECT * FROM BackendSettings WHERE id = ?',
            'bbbgbhfjge',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BackendSettings
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await BackendSettings.prisma().query_first(
            'SELECT * FROM BackendSettings WHERE apiUrl = ?',
            'igbehcbab',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.BackendSettingsCreateInput,
        include: Optional[types.BackendSettingsInclude] = None
    ) -> _PrismaModelT:
        """Create a new BackendSettings record.

        Parameters
        ----------
        data
            BackendSettings record data
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The created BackendSettings record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BackendSettings record from just the required fields
        backendsettings = await BackendSettings.prisma().create(
            data={
                # data to create a BackendSettings record
                'apiUrl': 'bdadaadhag',
                'dbPath': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.BackendSettingsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BackendSettings records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BackendSettings record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await BackendSettings.prisma().create_many(
            data=[
                {
                    # data to create a BackendSettings record
                    'apiUrl': 'caaaedabfc',
                    'dbPath': 'bigibebcib',
                },
                {
                    # data to create a BackendSettings record
                    'apiUrl': 'bigaiehgcc',
                    'dbPath': 'beeifcbebf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.BackendSettingsWhereUniqueInput,
        include: Optional[types.BackendSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BackendSettings record.

        Parameters
        ----------
        where
            BackendSettings filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The deleted BackendSettings record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        backendsettings = await BackendSettings.prisma().delete(
            where={
                'id': 'bgcigfahea',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.BackendSettingsWhereUniqueInput,
        include: Optional[types.BackendSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BackendSettings record.

        Parameters
        ----------
        where
            BackendSettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The found BackendSettings record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        backendsettings = await BackendSettings.prisma().find_unique(
            where={
                'id': 'bcejgaggif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.BackendSettingsWhereUniqueInput,
        include: Optional[types.BackendSettingsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BackendSettings record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BackendSettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The found BackendSettings record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        backendsettings = await BackendSettings.prisma().find_unique_or_raise(
            where={
                'id': 'idfjadbcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
        include: Optional[types.BackendSettingsInclude] = None,
        order: Optional[Union[types.BackendSettingsOrderByInput, List[types.BackendSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.BackendSettingsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BackendSettings records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BackendSettings records returned
        skip
            Ignore the first N results
        where
            BackendSettings filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BackendSettings model
        order
            Order the returned BackendSettings records by any field
        distinct
            Filter BackendSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BackendSettings]
            The list of all BackendSettings records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BackendSettings records
        backendsettings = await BackendSettings.prisma().find_many(take=10)

        # find the first 5 BackendSettings records ordered by the dbPath field
        backendsettings = await BackendSettings.prisma().find_many(
            take=5,
            order={
                'dbPath': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
        include: Optional[types.BackendSettingsInclude] = None,
        order: Optional[Union[types.BackendSettingsOrderByInput, List[types.BackendSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.BackendSettingsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BackendSettings record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BackendSettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BackendSettings model
        order
            Order the returned BackendSettings records by any field
        distinct
            Filter BackendSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BackendSettings
            The first BackendSettings record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BackendSettings record ordered by the id field
        backendsettings = await BackendSettings.prisma().find_first(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
        include: Optional[types.BackendSettingsInclude] = None,
        order: Optional[Union[types.BackendSettingsOrderByInput, List[types.BackendSettingsOrderByInput]]] = None,
        distinct: Optional[List[types.BackendSettingsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BackendSettings record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BackendSettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BackendSettings model
        order
            Order the returned BackendSettings records by any field
        distinct
            Filter BackendSettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BackendSettings
            The first BackendSettings record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BackendSettings record ordered by the apiUrl field
        backendsettings = await BackendSettings.prisma().find_first_or_raise(
            skip=1,
            order={
                'apiUrl': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.BackendSettingsUpdateInput,
        where: types.BackendSettingsWhereUniqueInput,
        include: Optional[types.BackendSettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BackendSettings record.

        Parameters
        ----------
        data
            BackendSettings record data specifying what to update
        where
            BackendSettings filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The updated BackendSettings record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        backendsettings = await BackendSettings.prisma().update(
            where={
                'id': 'hgdhbjhhj',
            },
            data={
                # data to update the BackendSettings record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.BackendSettingsWhereUniqueInput,
        data: types.BackendSettingsUpsertInput,
        include: Optional[types.BackendSettingsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BackendSettings filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BackendSettings model

        Returns
        -------
        prisma.models.BackendSettings
            The created or updated BackendSettings record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        backendsettings = await BackendSettings.prisma().upsert(
            where={
                'id': 'ecjjjfbae',
            },
            data={
                'create': {
                    'id': 'ecjjjfbae',
                    'apiUrl': 'bigaiehgcc',
                    'dbPath': 'beeifcbebf',
                },
                'update': {
                    'apiUrl': 'bigaiehgcc',
                    'dbPath': 'beeifcbebf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.BackendSettingsUpdateManyMutationInput,
        where: types.BackendSettingsWhereInput,
    ) -> int:
        """Update multiple BackendSettings records

        Parameters
        ----------
        data
            BackendSettings data to update the selected BackendSettings records to
        where
            Filter to select the BackendSettings records to update

        Returns
        -------
        int
            The total number of BackendSettings records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BackendSettings records
        total = await BackendSettings.prisma().update_many(
            data={
                'dbPath': 'bhhfibbigf'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BackendSettings records present in the database

        Parameters
        ----------
        select
            Select the BackendSettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BackendSettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BackendSettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BackendSettings.prisma().count()

        # results: prisma.types.BackendSettingsCountAggregateOutput
        results = await BackendSettings.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.BackendSettingsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
    ) -> types.BackendSettingsCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.BackendSettingsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BackendSettingsWhereInput] = None,
        cursor: Optional[types.BackendSettingsWhereUniqueInput] = None,
    ) -> Union[int, types.BackendSettingsCountAggregateOutput]:
        """Count the number of BackendSettings records present in the database

        Parameters
        ----------
        select
            Select the BackendSettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BackendSettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BackendSettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BackendSettings.prisma().count()

        # results: prisma.types.BackendSettingsCountAggregateOutput
        results = await BackendSettings.prisma().count(
            select={
                '_all': True,
                'apiUrl': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BackendSettingsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.BackendSettingsWhereInput] = None
    ) -> int:
        """Delete multiple BackendSettings records.

        Parameters
        ----------
        where
            Optional BackendSettings filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BackendSettings records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BackendSettings records
        total = await BackendSettings.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.BackendSettingsScalarFieldKeys'],
        *,
        where: Optional['types.BackendSettingsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BackendSettingsAvgAggregateInput'] = None,
        sum: Optional['types.BackendSettingsSumAggregateInput'] = None,
        min: Optional['types.BackendSettingsMinAggregateInput'] = None,
        max: Optional['types.BackendSettingsMaxAggregateInput'] = None,
        having: Optional['types.BackendSettingsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BackendSettingsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BackendSettingsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BackendSettingsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BackendSettingsGroupByOutput']:
        """Group BackendSettings records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BackendSettings fields to group records by
        where
            BackendSettings filter to select records
        take
            Limit the maximum number of BackendSettings records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BackendSettingsGroupByOutput]
            A list of dictionaries representing the BackendSettings record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BackendSettings records by dbPath values
        # and count how many records are in each group
        results = await BackendSettings.prisma().group_by(
            ['dbPath'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ComfyUISettingsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ComfyUISettings]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ComfyUISettings.prisma().query_raw(
            'SELECT * FROM ComfyUISettings WHERE id = ?',
            'ijdbeffgg',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ComfyUISettings
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ComfyUISettings.prisma().query_first(
            'SELECT * FROM ComfyUISettings WHERE ip = ?',
            'jjfeafhfj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ComfyUISettingsCreateInput,
        include: Optional[types.ComfyUISettingsInclude] = None
    ) -> _PrismaModelT:
        """Create a new ComfyUISettings record.

        Parameters
        ----------
        data
            ComfyUISettings record data
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The created ComfyUISettings record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ComfyUISettings record from just the required fields
        comfyuisettings = await ComfyUISettings.prisma().create(
            data={
                # data to create a ComfyUISettings record
                'ip': 'cbachdgfce',
                'port': 271520213,
                'deviceId': 'efggddide',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ComfyUISettingsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ComfyUISettings records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ComfyUISettings record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ComfyUISettings.prisma().create_many(
            data=[
                {
                    # data to create a ComfyUISettings record
                    'ip': 'caficfigfb',
                    'port': 1583689592,
                    'deviceId': 'ihieecagf',
                },
                {
                    # data to create a ComfyUISettings record
                    'ip': 'bghfciaafe',
                    'port': 1627576247,
                    'deviceId': 'cafeiaccbc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ComfyUISettingsWhereUniqueInput,
        include: Optional[types.ComfyUISettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ComfyUISettings record.

        Parameters
        ----------
        where
            ComfyUISettings filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The deleted ComfyUISettings record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyuisettings = await ComfyUISettings.prisma().delete(
            where={
                'id': 'gaddfhfh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ComfyUISettingsWhereUniqueInput,
        include: Optional[types.ComfyUISettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ComfyUISettings record.

        Parameters
        ----------
        where
            ComfyUISettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The found ComfyUISettings record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyuisettings = await ComfyUISettings.prisma().find_unique(
            where={
                'id': 'gieegcbeg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ComfyUISettingsWhereUniqueInput,
        include: Optional[types.ComfyUISettingsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ComfyUISettings record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ComfyUISettings filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The found ComfyUISettings record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyuisettings = await ComfyUISettings.prisma().find_unique_or_raise(
            where={
                'id': 'bgcffadich',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
        include: Optional[types.ComfyUISettingsInclude] = None,
        order: Optional[Union[types.ComfyUISettingsOrderByInput, List[types.ComfyUISettingsOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyUISettingsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ComfyUISettings records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ComfyUISettings records returned
        skip
            Ignore the first N results
        where
            ComfyUISettings filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model
        order
            Order the returned ComfyUISettings records by any field
        distinct
            Filter ComfyUISettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ComfyUISettings]
            The list of all ComfyUISettings records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ComfyUISettings records
        comfyuisettings = await ComfyUISettings.prisma().find_many(take=10)

        # find the first 5 ComfyUISettings records ordered by the port field
        comfyuisettings = await ComfyUISettings.prisma().find_many(
            take=5,
            order={
                'port': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
        include: Optional[types.ComfyUISettingsInclude] = None,
        order: Optional[Union[types.ComfyUISettingsOrderByInput, List[types.ComfyUISettingsOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyUISettingsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ComfyUISettings record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ComfyUISettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model
        order
            Order the returned ComfyUISettings records by any field
        distinct
            Filter ComfyUISettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ComfyUISettings
            The first ComfyUISettings record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ComfyUISettings record ordered by the deviceId field
        comfyuisettings = await ComfyUISettings.prisma().find_first(
            skip=1,
            order={
                'deviceId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
        include: Optional[types.ComfyUISettingsInclude] = None,
        order: Optional[Union[types.ComfyUISettingsOrderByInput, List[types.ComfyUISettingsOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyUISettingsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ComfyUISettings record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ComfyUISettings filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model
        order
            Order the returned ComfyUISettings records by any field
        distinct
            Filter ComfyUISettings records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ComfyUISettings
            The first ComfyUISettings record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ComfyUISettings record ordered by the pollInterval field
        comfyuisettings = await ComfyUISettings.prisma().find_first_or_raise(
            skip=1,
            order={
                'pollInterval': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ComfyUISettingsUpdateInput,
        where: types.ComfyUISettingsWhereUniqueInput,
        include: Optional[types.ComfyUISettingsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ComfyUISettings record.

        Parameters
        ----------
        data
            ComfyUISettings record data specifying what to update
        where
            ComfyUISettings filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The updated ComfyUISettings record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        comfyuisettings = await ComfyUISettings.prisma().update(
            where={
                'id': 'fcbichhci',
            },
            data={
                # data to update the ComfyUISettings record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ComfyUISettingsWhereUniqueInput,
        data: types.ComfyUISettingsUpsertInput,
        include: Optional[types.ComfyUISettingsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ComfyUISettings filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ComfyUISettings model

        Returns
        -------
        prisma.models.ComfyUISettings
            The created or updated ComfyUISettings record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyuisettings = await ComfyUISettings.prisma().upsert(
            where={
                'id': 'bcggadccgf',
            },
            data={
                'create': {
                    'id': 'bcggadccgf',
                    'ip': 'bghfciaafe',
                    'port': 1627576247,
                    'deviceId': 'cafeiaccbc',
                },
                'update': {
                    'ip': 'bghfciaafe',
                    'port': 1627576247,
                    'deviceId': 'cafeiaccbc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ComfyUISettingsUpdateManyMutationInput,
        where: types.ComfyUISettingsWhereInput,
    ) -> int:
        """Update multiple ComfyUISettings records

        Parameters
        ----------
        data
            ComfyUISettings data to update the selected ComfyUISettings records to
        where
            Filter to select the ComfyUISettings records to update

        Returns
        -------
        int
            The total number of ComfyUISettings records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ComfyUISettings records
        total = await ComfyUISettings.prisma().update_many(
            data={
                'id': 'jdcfdcgc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ComfyUISettings records present in the database

        Parameters
        ----------
        select
            Select the ComfyUISettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ComfyUISettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ComfyUISettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ComfyUISettings.prisma().count()

        # results: prisma.types.ComfyUISettingsCountAggregateOutput
        results = await ComfyUISettings.prisma().count(
            select={
                '_all': True,
                'ip': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ComfyUISettingsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
    ) -> types.ComfyUISettingsCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ComfyUISettingsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyUISettingsWhereInput] = None,
        cursor: Optional[types.ComfyUISettingsWhereUniqueInput] = None,
    ) -> Union[int, types.ComfyUISettingsCountAggregateOutput]:
        """Count the number of ComfyUISettings records present in the database

        Parameters
        ----------
        select
            Select the ComfyUISettings fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ComfyUISettings filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ComfyUISettingsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ComfyUISettings.prisma().count()

        # results: prisma.types.ComfyUISettingsCountAggregateOutput
        results = await ComfyUISettings.prisma().count(
            select={
                '_all': True,
                'port': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ComfyUISettingsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ComfyUISettingsWhereInput] = None
    ) -> int:
        """Delete multiple ComfyUISettings records.

        Parameters
        ----------
        where
            Optional ComfyUISettings filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ComfyUISettings records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ComfyUISettings records
        total = await ComfyUISettings.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ComfyUISettingsScalarFieldKeys'],
        *,
        where: Optional['types.ComfyUISettingsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ComfyUISettingsAvgAggregateInput'] = None,
        sum: Optional['types.ComfyUISettingsSumAggregateInput'] = None,
        min: Optional['types.ComfyUISettingsMinAggregateInput'] = None,
        max: Optional['types.ComfyUISettingsMaxAggregateInput'] = None,
        having: Optional['types.ComfyUISettingsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ComfyUISettingsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ComfyUISettingsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ComfyUISettingsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ComfyUISettingsGroupByOutput']:
        """Group ComfyUISettings records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ComfyUISettings fields to group records by
        where
            ComfyUISettings filter to select records
        take
            Limit the maximum number of ComfyUISettings records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ComfyUISettingsGroupByOutput]
            A list of dictionaries representing the ComfyUISettings record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ComfyUISettings records by deviceId values
        # and count how many records are in each group
        results = await ComfyUISettings.prisma().group_by(
            ['deviceId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ProjectActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Project]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Project.prisma().query_raw(
            'SELECT * FROM Project WHERE id = ?',
            'cafdaehjid',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Project
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Project.prisma().query_first(
            'SELECT * FROM Project WHERE name = ?',
            'gifdddbia',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ProjectCreateInput,
        include: Optional[types.ProjectInclude] = None
    ) -> _PrismaModelT:
        """Create a new Project record.

        Parameters
        ----------
        data
            Project record data
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The created Project record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Project record from just the required fields
        project = await Project.prisma().create(
            data={
                # data to create a Project record
                'name': 'bchehecef',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ProjectCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Project records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Project record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Project.prisma().create_many(
            data=[
                {
                    # data to create a Project record
                    'name': 'jeijcbhfe',
                },
                {
                    # data to create a Project record
                    'name': 'bjgejjabff',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ProjectWhereUniqueInput,
        include: Optional[types.ProjectInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Project record.

        Parameters
        ----------
        where
            Project filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The deleted Project record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        project = await Project.prisma().delete(
            where={
                'id': 'bcciijbibg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ProjectWhereUniqueInput,
        include: Optional[types.ProjectInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Project record.

        Parameters
        ----------
        where
            Project filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The found Project record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        project = await Project.prisma().find_unique(
            where={
                'id': 'cffcachfd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ProjectWhereUniqueInput,
        include: Optional[types.ProjectInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Project record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Project filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The found Project record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        project = await Project.prisma().find_unique_or_raise(
            where={
                'id': 'bccdfhdigc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
        include: Optional[types.ProjectInclude] = None,
        order: Optional[Union[types.ProjectOrderByInput, List[types.ProjectOrderByInput]]] = None,
        distinct: Optional[List[types.ProjectScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Project records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Project records returned
        skip
            Ignore the first N results
        where
            Project filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Project model
        order
            Order the returned Project records by any field
        distinct
            Filter Project records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Project]
            The list of all Project records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Project records
        projects = await Project.prisma().find_many(take=10)

        # find the first 5 Project records ordered by the type field
        projects = await Project.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
        include: Optional[types.ProjectInclude] = None,
        order: Optional[Union[types.ProjectOrderByInput, List[types.ProjectOrderByInput]]] = None,
        distinct: Optional[List[types.ProjectScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Project record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Project filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Project model
        order
            Order the returned Project records by any field
        distinct
            Filter Project records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Project
            The first Project record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Project record ordered by the duration field
        project = await Project.prisma().find_first(
            skip=1,
            order={
                'duration': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
        include: Optional[types.ProjectInclude] = None,
        order: Optional[Union[types.ProjectOrderByInput, List[types.ProjectOrderByInput]]] = None,
        distinct: Optional[List[types.ProjectScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Project record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Project filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Project model
        order
            Order the returned Project records by any field
        distinct
            Filter Project records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Project
            The first Project record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Project record ordered by the episodeCount field
        project = await Project.prisma().find_first_or_raise(
            skip=1,
            order={
                'episodeCount': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ProjectUpdateInput,
        where: types.ProjectWhereUniqueInput,
        include: Optional[types.ProjectInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Project record.

        Parameters
        ----------
        data
            Project record data specifying what to update
        where
            Project filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The updated Project record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        project = await Project.prisma().update(
            where={
                'id': 'febcgjbfj',
            },
            data={
                # data to update the Project record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ProjectWhereUniqueInput,
        data: types.ProjectUpsertInput,
        include: Optional[types.ProjectInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Project filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Project model

        Returns
        -------
        prisma.models.Project
            The created or updated Project record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        project = await Project.prisma().upsert(
            where={
                'id': 'bageiegghg',
            },
            data={
                'create': {
                    'id': 'bageiegghg',
                    'name': 'bjgejjabff',
                },
                'update': {
                    'name': 'bjgejjabff',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ProjectUpdateManyMutationInput,
        where: types.ProjectWhereInput,
    ) -> int:
        """Update multiple Project records

        Parameters
        ----------
        data
            Project data to update the selected Project records to
        where
            Filter to select the Project records to update

        Returns
        -------
        int
            The total number of Project records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Project records
        total = await Project.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Project records present in the database

        Parameters
        ----------
        select
            Select the Project fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Project filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProjectCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Project.prisma().count()

        # results: prisma.types.ProjectCountAggregateOutput
        results = await Project.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ProjectCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
    ) -> types.ProjectCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ProjectCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ProjectWhereInput] = None,
        cursor: Optional[types.ProjectWhereUniqueInput] = None,
    ) -> Union[int, types.ProjectCountAggregateOutput]:
        """Count the number of Project records present in the database

        Parameters
        ----------
        select
            Select the Project fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Project filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ProjectCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Project.prisma().count()

        # results: prisma.types.ProjectCountAggregateOutput
        results = await Project.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ProjectCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ProjectWhereInput] = None
    ) -> int:
        """Delete multiple Project records.

        Parameters
        ----------
        where
            Optional Project filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Project records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Project records
        total = await Project.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ProjectScalarFieldKeys'],
        *,
        where: Optional['types.ProjectWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ProjectAvgAggregateInput'] = None,
        sum: Optional['types.ProjectSumAggregateInput'] = None,
        min: Optional['types.ProjectMinAggregateInput'] = None,
        max: Optional['types.ProjectMaxAggregateInput'] = None,
        having: Optional['types.ProjectScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ProjectCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ProjectScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ProjectScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ProjectGroupByOutput']:
        """Group Project records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Project fields to group records by
        where
            Project filter to select records
        take
            Limit the maximum number of Project records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ProjectGroupByOutput]
            A list of dictionaries representing the Project record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Project records by name values
        # and count how many records are in each group
        results = await Project.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StoryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Story]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Story.prisma().query_raw(
            'SELECT * FROM Story WHERE id = ?',
            'faidicegb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Story
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Story.prisma().query_first(
            'SELECT * FROM Story WHERE projectId = ?',
            'bacecgfhbe',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.StoryCreateInput,
        include: Optional[types.StoryInclude] = None
    ) -> _PrismaModelT:
        """Create a new Story record.

        Parameters
        ----------
        data
            Story record data
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The created Story record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Story record from just the required fields
        story = await Story.prisma().create(
            data={
                # data to create a Story record
                'projectId': 'ihcahiead',
                'narrativeArc': 'biheheiajg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.StoryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Story records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Story record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Story.prisma().create_many(
            data=[
                {
                    # data to create a Story record
                    'projectId': 'jbgijghgb',
                    'narrativeArc': 'hgjcghfbi',
                },
                {
                    # data to create a Story record
                    'projectId': 'icadbcehj',
                    'narrativeArc': 'jchciaee',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.StoryWhereUniqueInput,
        include: Optional[types.StoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Story record.

        Parameters
        ----------
        where
            Story filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The deleted Story record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        story = await Story.prisma().delete(
            where={
                'id': 'deeificjd',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.StoryWhereUniqueInput,
        include: Optional[types.StoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Story record.

        Parameters
        ----------
        where
            Story filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The found Story record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        story = await Story.prisma().find_unique(
            where={
                'id': 'bbcbhebbda',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.StoryWhereUniqueInput,
        include: Optional[types.StoryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Story record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Story filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The found Story record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        story = await Story.prisma().find_unique_or_raise(
            where={
                'id': 'bejfijgcfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
        include: Optional[types.StoryInclude] = None,
        order: Optional[Union[types.StoryOrderByInput, List[types.StoryOrderByInput]]] = None,
        distinct: Optional[List[types.StoryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Story records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Story records returned
        skip
            Ignore the first N results
        where
            Story filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Story model
        order
            Order the returned Story records by any field
        distinct
            Filter Story records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Story]
            The list of all Story records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Story records
        storys = await Story.prisma().find_many(take=10)

        # find the first 5 Story records ordered by the narrativeArc field
        storys = await Story.prisma().find_many(
            take=5,
            order={
                'narrativeArc': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
        include: Optional[types.StoryInclude] = None,
        order: Optional[Union[types.StoryOrderByInput, List[types.StoryOrderByInput]]] = None,
        distinct: Optional[List[types.StoryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Story record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Story filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Story model
        order
            Order the returned Story records by any field
        distinct
            Filter Story records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Story
            The first Story record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Story record ordered by the rawInput field
        story = await Story.prisma().find_first(
            skip=1,
            order={
                'rawInput': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
        include: Optional[types.StoryInclude] = None,
        order: Optional[Union[types.StoryOrderByInput, List[types.StoryOrderByInput]]] = None,
        distinct: Optional[List[types.StoryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Story record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Story filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Story model
        order
            Order the returned Story records by any field
        distinct
            Filter Story records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Story
            The first Story record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Story record ordered by the id field
        story = await Story.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.StoryUpdateInput,
        where: types.StoryWhereUniqueInput,
        include: Optional[types.StoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Story record.

        Parameters
        ----------
        data
            Story record data specifying what to update
        where
            Story filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The updated Story record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        story = await Story.prisma().update(
            where={
                'id': 'caifcbgii',
            },
            data={
                # data to update the Story record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.StoryWhereUniqueInput,
        data: types.StoryUpsertInput,
        include: Optional[types.StoryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Story filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Story model

        Returns
        -------
        prisma.models.Story
            The created or updated Story record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        story = await Story.prisma().upsert(
            where={
                'id': 'igaibbfgj',
            },
            data={
                'create': {
                    'id': 'igaibbfgj',
                    'projectId': 'icadbcehj',
                    'narrativeArc': 'jchciaee',
                },
                'update': {
                    'projectId': 'icadbcehj',
                    'narrativeArc': 'jchciaee',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.StoryUpdateManyMutationInput,
        where: types.StoryWhereInput,
    ) -> int:
        """Update multiple Story records

        Parameters
        ----------
        data
            Story data to update the selected Story records to
        where
            Filter to select the Story records to update

        Returns
        -------
        int
            The total number of Story records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Story records
        total = await Story.prisma().update_many(
            data={
                'projectId': 'bggajdcbbi'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Story records present in the database

        Parameters
        ----------
        select
            Select the Story fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Story filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Story.prisma().count()

        # results: prisma.types.StoryCountAggregateOutput
        results = await Story.prisma().count(
            select={
                '_all': True,
                'narrativeArc': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.StoryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
    ) -> types.StoryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.StoryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryWhereInput] = None,
        cursor: Optional[types.StoryWhereUniqueInput] = None,
    ) -> Union[int, types.StoryCountAggregateOutput]:
        """Count the number of Story records present in the database

        Parameters
        ----------
        select
            Select the Story fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Story filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Story.prisma().count()

        # results: prisma.types.StoryCountAggregateOutput
        results = await Story.prisma().count(
            select={
                '_all': True,
                'rawInput': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StoryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.StoryWhereInput] = None
    ) -> int:
        """Delete multiple Story records.

        Parameters
        ----------
        where
            Optional Story filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Story records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Story records
        total = await Story.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.StoryScalarFieldKeys'],
        *,
        where: Optional['types.StoryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StoryAvgAggregateInput'] = None,
        sum: Optional['types.StorySumAggregateInput'] = None,
        min: Optional['types.StoryMinAggregateInput'] = None,
        max: Optional['types.StoryMaxAggregateInput'] = None,
        having: Optional['types.StoryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StoryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StoryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StoryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StoryGroupByOutput']:
        """Group Story records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Story fields to group records by
        where
            Story filter to select records
        take
            Limit the maximum number of Story records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StoryGroupByOutput]
            A list of dictionaries representing the Story record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Story records by id values
        # and count how many records are in each group
        results = await Story.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BeatActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Beat]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Beat.prisma().query_raw(
            'SELECT * FROM Beat WHERE id = ?',
            'fcfhgbjed',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Beat
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Beat.prisma().query_first(
            'SELECT * FROM Beat WHERE projectId = ?',
            'hdgcajhjg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.BeatCreateInput,
        include: Optional[types.BeatInclude] = None
    ) -> _PrismaModelT:
        """Create a new Beat record.

        Parameters
        ----------
        data
            Beat record data
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The created Beat record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Beat record from just the required fields
        beat = await Beat.prisma().create(
            data={
                # data to create a Beat record
                'projectId': 'ejdjahicb',
                'order': 639686562,
                'content': 'gfeaahdeh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.BeatCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Beat records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Beat record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Beat.prisma().create_many(
            data=[
                {
                    # data to create a Beat record
                    'projectId': 'bjafcgbffc',
                    'order': 78746985,
                    'content': 'bdjidcidac',
                },
                {
                    # data to create a Beat record
                    'projectId': 'ifgaaagff',
                    'order': 1452336924,
                    'content': 'bfhdbjjgfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.BeatWhereUniqueInput,
        include: Optional[types.BeatInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Beat record.

        Parameters
        ----------
        where
            Beat filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The deleted Beat record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        beat = await Beat.prisma().delete(
            where={
                'id': 'cabdjadaji',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.BeatWhereUniqueInput,
        include: Optional[types.BeatInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Beat record.

        Parameters
        ----------
        where
            Beat filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The found Beat record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        beat = await Beat.prisma().find_unique(
            where={
                'id': 'faajgfadf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.BeatWhereUniqueInput,
        include: Optional[types.BeatInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Beat record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Beat filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The found Beat record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        beat = await Beat.prisma().find_unique_or_raise(
            where={
                'id': 'biaagcedjc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
        include: Optional[types.BeatInclude] = None,
        order: Optional[Union[types.BeatOrderByInput, List[types.BeatOrderByInput]]] = None,
        distinct: Optional[List[types.BeatScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Beat records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Beat records returned
        skip
            Ignore the first N results
        where
            Beat filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Beat model
        order
            Order the returned Beat records by any field
        distinct
            Filter Beat records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Beat]
            The list of all Beat records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Beat records
        beats = await Beat.prisma().find_many(take=10)

        # find the first 5 Beat records ordered by the storyId field
        beats = await Beat.prisma().find_many(
            take=5,
            order={
                'storyId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
        include: Optional[types.BeatInclude] = None,
        order: Optional[Union[types.BeatOrderByInput, List[types.BeatOrderByInput]]] = None,
        distinct: Optional[List[types.BeatScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Beat record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Beat filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Beat model
        order
            Order the returned Beat records by any field
        distinct
            Filter Beat records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Beat
            The first Beat record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Beat record ordered by the order field
        beat = await Beat.prisma().find_first(
            skip=1,
            order={
                'order': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
        include: Optional[types.BeatInclude] = None,
        order: Optional[Union[types.BeatOrderByInput, List[types.BeatOrderByInput]]] = None,
        distinct: Optional[List[types.BeatScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Beat record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Beat filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Beat model
        order
            Order the returned Beat records by any field
        distinct
            Filter Beat records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Beat
            The first Beat record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Beat record ordered by the content field
        beat = await Beat.prisma().find_first_or_raise(
            skip=1,
            order={
                'content': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.BeatUpdateInput,
        where: types.BeatWhereUniqueInput,
        include: Optional[types.BeatInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Beat record.

        Parameters
        ----------
        data
            Beat record data specifying what to update
        where
            Beat filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The updated Beat record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        beat = await Beat.prisma().update(
            where={
                'id': 'cahhaghecf',
            },
            data={
                # data to update the Beat record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.BeatWhereUniqueInput,
        data: types.BeatUpsertInput,
        include: Optional[types.BeatInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Beat filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Beat model

        Returns
        -------
        prisma.models.Beat
            The created or updated Beat record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        beat = await Beat.prisma().upsert(
            where={
                'id': 'bghcbbcidi',
            },
            data={
                'create': {
                    'id': 'bghcbbcidi',
                    'projectId': 'ifgaaagff',
                    'order': 1452336924,
                    'content': 'bfhdbjjgfd',
                },
                'update': {
                    'projectId': 'ifgaaagff',
                    'order': 1452336924,
                    'content': 'bfhdbjjgfd',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.BeatUpdateManyMutationInput,
        where: types.BeatWhereInput,
    ) -> int:
        """Update multiple Beat records

        Parameters
        ----------
        data
            Beat data to update the selected Beat records to
        where
            Filter to select the Beat records to update

        Returns
        -------
        int
            The total number of Beat records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Beat records
        total = await Beat.prisma().update_many(
            data={
                'id': 'jcgghhgdj'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Beat records present in the database

        Parameters
        ----------
        select
            Select the Beat fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Beat filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BeatCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Beat.prisma().count()

        # results: prisma.types.BeatCountAggregateOutput
        results = await Beat.prisma().count(
            select={
                '_all': True,
                'projectId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.BeatCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
    ) -> types.BeatCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.BeatCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BeatWhereInput] = None,
        cursor: Optional[types.BeatWhereUniqueInput] = None,
    ) -> Union[int, types.BeatCountAggregateOutput]:
        """Count the number of Beat records present in the database

        Parameters
        ----------
        select
            Select the Beat fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Beat filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BeatCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Beat.prisma().count()

        # results: prisma.types.BeatCountAggregateOutput
        results = await Beat.prisma().count(
            select={
                '_all': True,
                'storyId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BeatCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.BeatWhereInput] = None
    ) -> int:
        """Delete multiple Beat records.

        Parameters
        ----------
        where
            Optional Beat filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Beat records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Beat records
        total = await Beat.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.BeatScalarFieldKeys'],
        *,
        where: Optional['types.BeatWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BeatAvgAggregateInput'] = None,
        sum: Optional['types.BeatSumAggregateInput'] = None,
        min: Optional['types.BeatMinAggregateInput'] = None,
        max: Optional['types.BeatMaxAggregateInput'] = None,
        having: Optional['types.BeatScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BeatCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BeatScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BeatScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BeatGroupByOutput']:
        """Group Beat records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Beat fields to group records by
        where
            Beat filter to select records
        take
            Limit the maximum number of Beat records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BeatGroupByOutput]
            A list of dictionaries representing the Beat record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Beat records by order values
        # and count how many records are in each group
        results = await Beat.prisma().group_by(
            ['order'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AssetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Asset]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Asset.prisma().query_raw(
            'SELECT * FROM Asset WHERE id = ?',
            'beehgcebbg',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Asset
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Asset.prisma().query_first(
            'SELECT * FROM Asset WHERE projectId = ?',
            'bhdiaidiaf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AssetCreateInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Create a new Asset record.

        Parameters
        ----------
        data
            Asset record data
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created Asset record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Asset record from just the required fields
        asset = await Asset.prisma().create(
            data={
                # data to create a Asset record
                'projectId': 'deajegcfi',
                'type': 'gabahhhjf',
                'name': 'cjagadcjg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AssetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Asset records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Asset record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Asset.prisma().create_many(
            data=[
                {
                    # data to create a Asset record
                    'projectId': 'bifficggej',
                    'type': 'bgbbaajbic',
                    'name': 'eegghdhjb',
                },
                {
                    # data to create a Asset record
                    'projectId': 'daafgidjg',
                    'type': 'gdcgcgagj',
                    'name': 'bhceabbgja',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Asset record.

        Parameters
        ----------
        where
            Asset filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The deleted Asset record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().delete(
            where={
                'id': 'ehabfhegh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Asset record.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().find_unique(
            where={
                'id': 'bcajcajjbc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Asset record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Asset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The found Asset record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().find_unique_or_raise(
            where={
                'id': 'bfdgheeegf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Asset records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N results
        where
            Asset filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Asset]
            The list of all Asset records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Asset records
        assets = await Asset.prisma().find_many(take=10)

        # find the first 5 Asset records ordered by the beatId field
        assets = await Asset.prisma().find_many(
            take=5,
            order={
                'beatId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Asset record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the type field
        asset = await Asset.prisma().find_first(
            skip=1,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
        include: Optional[types.AssetInclude] = None,
        order: Optional[Union[types.AssetOrderByInput, List[types.AssetOrderByInput]]] = None,
        distinct: Optional[List[types.AssetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Asset record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Asset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Asset model
        order
            Order the returned Asset records by any field
        distinct
            Filter Asset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Asset
            The first Asset record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Asset record ordered by the name field
        asset = await Asset.prisma().find_first_or_raise(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AssetUpdateInput,
        where: types.AssetWhereUniqueInput,
        include: Optional[types.AssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Asset record.

        Parameters
        ----------
        data
            Asset record data specifying what to update
        where
            Asset filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The updated Asset record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        asset = await Asset.prisma().update(
            where={
                'id': 'ececbijji',
            },
            data={
                # data to update the Asset record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AssetWhereUniqueInput,
        data: types.AssetUpsertInput,
        include: Optional[types.AssetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Asset filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Asset model

        Returns
        -------
        prisma.models.Asset
            The created or updated Asset record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        asset = await Asset.prisma().upsert(
            where={
                'id': 'cbcfgdcdhf',
            },
            data={
                'create': {
                    'id': 'cbcfgdcdhf',
                    'projectId': 'daafgidjg',
                    'type': 'gdcgcgagj',
                    'name': 'bhceabbgja',
                },
                'update': {
                    'projectId': 'daafgidjg',
                    'type': 'gdcgcgagj',
                    'name': 'bhceabbgja',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AssetUpdateManyMutationInput,
        where: types.AssetWhereInput,
    ) -> int:
        """Update multiple Asset records

        Parameters
        ----------
        data
            Asset data to update the selected Asset records to
        where
            Filter to select the Asset records to update

        Returns
        -------
        int
            The total number of Asset records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Asset records
        total = await Asset.prisma().update_many(
            data={
                'description': 'fdgjfbhia'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = await Asset.prisma().count(
            select={
                '_all': True,
                'imagePath': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AssetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> types.AssetCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AssetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetWhereInput] = None,
        cursor: Optional[types.AssetWhereUniqueInput] = None,
    ) -> Union[int, types.AssetCountAggregateOutput]:
        """Count the number of Asset records present in the database

        Parameters
        ----------
        select
            Select the Asset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Asset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Asset.prisma().count()

        # results: prisma.types.AssetCountAggregateOutput
        results = await Asset.prisma().count(
            select={
                '_all': True,
                'prompt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AssetCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AssetWhereInput] = None
    ) -> int:
        """Delete multiple Asset records.

        Parameters
        ----------
        where
            Optional Asset filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Asset records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Asset records
        total = await Asset.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AssetScalarFieldKeys'],
        *,
        where: Optional['types.AssetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AssetAvgAggregateInput'] = None,
        sum: Optional['types.AssetSumAggregateInput'] = None,
        min: Optional['types.AssetMinAggregateInput'] = None,
        max: Optional['types.AssetMaxAggregateInput'] = None,
        having: Optional['types.AssetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AssetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AssetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AssetGroupByOutput']:
        """Group Asset records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Asset fields to group records by
        where
            Asset filter to select records
        take
            Limit the maximum number of Asset records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AssetGroupByOutput]
            A list of dictionaries representing the Asset record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Asset records by createdAt values
        # and count how many records are in each group
        results = await Asset.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AssetStateActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AssetState]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AssetState.prisma().query_raw(
            'SELECT * FROM AssetState WHERE id = ?',
            'jcehcdchh',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AssetState
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AssetState.prisma().query_first(
            'SELECT * FROM AssetState WHERE assetId = ?',
            'bgcbjdhjcc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AssetStateCreateInput,
        include: Optional[types.AssetStateInclude] = None
    ) -> _PrismaModelT:
        """Create a new AssetState record.

        Parameters
        ----------
        data
            AssetState record data
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The created AssetState record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AssetState record from just the required fields
        assetstate = await AssetState.prisma().create(
            data={
                # data to create a AssetState record
                'assetId': 'bieiidcabj',
                'name': 'bjcbfcieaa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AssetStateCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AssetState records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AssetState record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AssetState.prisma().create_many(
            data=[
                {
                    # data to create a AssetState record
                    'assetId': 'cbaaechiej',
                    'name': 'iejbeaaeg',
                },
                {
                    # data to create a AssetState record
                    'assetId': 'jcibfcbhf',
                    'name': 'chdadcaga',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AssetStateWhereUniqueInput,
        include: Optional[types.AssetStateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AssetState record.

        Parameters
        ----------
        where
            AssetState filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The deleted AssetState record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetstate = await AssetState.prisma().delete(
            where={
                'id': 'jicieifbh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AssetStateWhereUniqueInput,
        include: Optional[types.AssetStateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AssetState record.

        Parameters
        ----------
        where
            AssetState filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The found AssetState record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetstate = await AssetState.prisma().find_unique(
            where={
                'id': 'fbahdheji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AssetStateWhereUniqueInput,
        include: Optional[types.AssetStateInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AssetState record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AssetState filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The found AssetState record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetstate = await AssetState.prisma().find_unique_or_raise(
            where={
                'id': 'cbbheiicgh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
        include: Optional[types.AssetStateInclude] = None,
        order: Optional[Union[types.AssetStateOrderByInput, List[types.AssetStateOrderByInput]]] = None,
        distinct: Optional[List[types.AssetStateScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AssetState records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AssetState records returned
        skip
            Ignore the first N results
        where
            AssetState filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetState model
        order
            Order the returned AssetState records by any field
        distinct
            Filter AssetState records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AssetState]
            The list of all AssetState records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AssetState records
        assetstates = await AssetState.prisma().find_many(take=10)

        # find the first 5 AssetState records ordered by the name field
        assetstates = await AssetState.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
        include: Optional[types.AssetStateInclude] = None,
        order: Optional[Union[types.AssetStateOrderByInput, List[types.AssetStateOrderByInput]]] = None,
        distinct: Optional[List[types.AssetStateScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AssetState record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AssetState filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetState model
        order
            Order the returned AssetState records by any field
        distinct
            Filter AssetState records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AssetState
            The first AssetState record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AssetState record ordered by the description field
        assetstate = await AssetState.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
        include: Optional[types.AssetStateInclude] = None,
        order: Optional[Union[types.AssetStateOrderByInput, List[types.AssetStateOrderByInput]]] = None,
        distinct: Optional[List[types.AssetStateScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AssetState record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AssetState filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AssetState model
        order
            Order the returned AssetState records by any field
        distinct
            Filter AssetState records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AssetState
            The first AssetState record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AssetState record ordered by the prompt field
        assetstate = await AssetState.prisma().find_first_or_raise(
            skip=1,
            order={
                'prompt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AssetStateUpdateInput,
        where: types.AssetStateWhereUniqueInput,
        include: Optional[types.AssetStateInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AssetState record.

        Parameters
        ----------
        data
            AssetState record data specifying what to update
        where
            AssetState filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The updated AssetState record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        assetstate = await AssetState.prisma().update(
            where={
                'id': 'beabjeejdg',
            },
            data={
                # data to update the AssetState record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AssetStateWhereUniqueInput,
        data: types.AssetStateUpsertInput,
        include: Optional[types.AssetStateInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AssetState filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AssetState model

        Returns
        -------
        prisma.models.AssetState
            The created or updated AssetState record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        assetstate = await AssetState.prisma().upsert(
            where={
                'id': 'bcjhgahffd',
            },
            data={
                'create': {
                    'id': 'bcjhgahffd',
                    'assetId': 'jcibfcbhf',
                    'name': 'chdadcaga',
                },
                'update': {
                    'assetId': 'jcibfcbhf',
                    'name': 'chdadcaga',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AssetStateUpdateManyMutationInput,
        where: types.AssetStateWhereInput,
    ) -> int:
        """Update multiple AssetState records

        Parameters
        ----------
        data
            AssetState data to update the selected AssetState records to
        where
            Filter to select the AssetState records to update

        Returns
        -------
        int
            The total number of AssetState records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AssetState records
        total = await AssetState.prisma().update_many(
            data={
                'imagePath': 'fbjeiiffa'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AssetState records present in the database

        Parameters
        ----------
        select
            Select the AssetState fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AssetState filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetStateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AssetState.prisma().count()

        # results: prisma.types.AssetStateCountAggregateOutput
        results = await AssetState.prisma().count(
            select={
                '_all': True,
                'characterSheet': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AssetStateCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
    ) -> types.AssetStateCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AssetStateCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AssetStateWhereInput] = None,
        cursor: Optional[types.AssetStateWhereUniqueInput] = None,
    ) -> Union[int, types.AssetStateCountAggregateOutput]:
        """Count the number of AssetState records present in the database

        Parameters
        ----------
        select
            Select the AssetState fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AssetState filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AssetStateCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AssetState.prisma().count()

        # results: prisma.types.AssetStateCountAggregateOutput
        results = await AssetState.prisma().count(
            select={
                '_all': True,
                'scenes': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AssetStateCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AssetStateWhereInput] = None
    ) -> int:
        """Delete multiple AssetState records.

        Parameters
        ----------
        where
            Optional AssetState filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AssetState records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AssetState records
        total = await AssetState.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AssetStateScalarFieldKeys'],
        *,
        where: Optional['types.AssetStateWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AssetStateAvgAggregateInput'] = None,
        sum: Optional['types.AssetStateSumAggregateInput'] = None,
        min: Optional['types.AssetStateMinAggregateInput'] = None,
        max: Optional['types.AssetStateMaxAggregateInput'] = None,
        having: Optional['types.AssetStateScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AssetStateCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AssetStateScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AssetStateScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AssetStateGroupByOutput']:
        """Group AssetState records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AssetState fields to group records by
        where
            AssetState filter to select records
        take
            Limit the maximum number of AssetState records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AssetStateGroupByOutput]
            A list of dictionaries representing the AssetState record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AssetState records by createdAt values
        # and count how many records are in each group
        results = await AssetState.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ShotActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Shot]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Shot.prisma().query_raw(
            'SELECT * FROM Shot WHERE id = ?',
            'jhgidcgbf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Shot
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Shot.prisma().query_first(
            'SELECT * FROM Shot WHERE beatId = ?',
            'bgjgecfejc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ShotCreateInput,
        include: Optional[types.ShotInclude] = None
    ) -> _PrismaModelT:
        """Create a new Shot record.

        Parameters
        ----------
        data
            Shot record data
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The created Shot record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Shot record from just the required fields
        shot = await Shot.prisma().create(
            data={
                # data to create a Shot record
                'beatId': 'bgjcgchib',
                'order': 1023081650,
                'prompt': 'dchgibach',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ShotCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Shot records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Shot record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Shot.prisma().create_many(
            data=[
                {
                    # data to create a Shot record
                    'beatId': 'fchheijjc',
                    'order': 2029357497,
                    'prompt': 'bdbifjhbbi',
                },
                {
                    # data to create a Shot record
                    'beatId': 'cbccbbcdfb',
                    'order': 1024943020,
                    'prompt': 'bhbhdahfaj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ShotWhereUniqueInput,
        include: Optional[types.ShotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Shot record.

        Parameters
        ----------
        where
            Shot filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The deleted Shot record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shot = await Shot.prisma().delete(
            where={
                'id': 'bfjibceaec',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ShotWhereUniqueInput,
        include: Optional[types.ShotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Shot record.

        Parameters
        ----------
        where
            Shot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The found Shot record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shot = await Shot.prisma().find_unique(
            where={
                'id': 'ibhgcdbgd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ShotWhereUniqueInput,
        include: Optional[types.ShotInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Shot record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Shot filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The found Shot record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shot = await Shot.prisma().find_unique_or_raise(
            where={
                'id': 'badaffhddg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
        include: Optional[types.ShotInclude] = None,
        order: Optional[Union[types.ShotOrderByInput, List[types.ShotOrderByInput]]] = None,
        distinct: Optional[List[types.ShotScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Shot records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Shot records returned
        skip
            Ignore the first N results
        where
            Shot filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Shot model
        order
            Order the returned Shot records by any field
        distinct
            Filter Shot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Shot]
            The list of all Shot records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Shot records
        shots = await Shot.prisma().find_many(take=10)

        # find the first 5 Shot records ordered by the order field
        shots = await Shot.prisma().find_many(
            take=5,
            order={
                'order': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
        include: Optional[types.ShotInclude] = None,
        order: Optional[Union[types.ShotOrderByInput, List[types.ShotOrderByInput]]] = None,
        distinct: Optional[List[types.ShotScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Shot record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Shot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Shot model
        order
            Order the returned Shot records by any field
        distinct
            Filter Shot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Shot
            The first Shot record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Shot record ordered by the prompt field
        shot = await Shot.prisma().find_first(
            skip=1,
            order={
                'prompt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
        include: Optional[types.ShotInclude] = None,
        order: Optional[Union[types.ShotOrderByInput, List[types.ShotOrderByInput]]] = None,
        distinct: Optional[List[types.ShotScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Shot record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Shot filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Shot model
        order
            Order the returned Shot records by any field
        distinct
            Filter Shot records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Shot
            The first Shot record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Shot record ordered by the cameraMovement field
        shot = await Shot.prisma().find_first_or_raise(
            skip=1,
            order={
                'cameraMovement': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ShotUpdateInput,
        where: types.ShotWhereUniqueInput,
        include: Optional[types.ShotInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Shot record.

        Parameters
        ----------
        data
            Shot record data specifying what to update
        where
            Shot filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The updated Shot record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        shot = await Shot.prisma().update(
            where={
                'id': 'bbdbfcfihd',
            },
            data={
                # data to update the Shot record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ShotWhereUniqueInput,
        data: types.ShotUpsertInput,
        include: Optional[types.ShotInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Shot filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Shot model

        Returns
        -------
        prisma.models.Shot
            The created or updated Shot record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shot = await Shot.prisma().upsert(
            where={
                'id': 'cbagggbji',
            },
            data={
                'create': {
                    'id': 'cbagggbji',
                    'beatId': 'cbccbbcdfb',
                    'order': 1024943020,
                    'prompt': 'bhbhdahfaj',
                },
                'update': {
                    'beatId': 'cbccbbcdfb',
                    'order': 1024943020,
                    'prompt': 'bhbhdahfaj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ShotUpdateManyMutationInput,
        where: types.ShotWhereInput,
    ) -> int:
        """Update multiple Shot records

        Parameters
        ----------
        data
            Shot data to update the selected Shot records to
        where
            Filter to select the Shot records to update

        Returns
        -------
        int
            The total number of Shot records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Shot records
        total = await Shot.prisma().update_many(
            data={
                'duration': 1276057943.74556
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Shot records present in the database

        Parameters
        ----------
        select
            Select the Shot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Shot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ShotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Shot.prisma().count()

        # results: prisma.types.ShotCountAggregateOutput
        results = await Shot.prisma().count(
            select={
                '_all': True,
                'videoPath': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ShotCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
    ) -> types.ShotCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ShotCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotWhereInput] = None,
        cursor: Optional[types.ShotWhereUniqueInput] = None,
    ) -> Union[int, types.ShotCountAggregateOutput]:
        """Count the number of Shot records present in the database

        Parameters
        ----------
        select
            Select the Shot fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Shot filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ShotCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Shot.prisma().count()

        # results: prisma.types.ShotCountAggregateOutput
        results = await Shot.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ShotCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ShotWhereInput] = None
    ) -> int:
        """Delete multiple Shot records.

        Parameters
        ----------
        where
            Optional Shot filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Shot records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Shot records
        total = await Shot.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ShotScalarFieldKeys'],
        *,
        where: Optional['types.ShotWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ShotAvgAggregateInput'] = None,
        sum: Optional['types.ShotSumAggregateInput'] = None,
        min: Optional['types.ShotMinAggregateInput'] = None,
        max: Optional['types.ShotMaxAggregateInput'] = None,
        having: Optional['types.ShotScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ShotCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ShotScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ShotScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ShotGroupByOutput']:
        """Group Shot records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Shot fields to group records by
        where
            Shot filter to select records
        take
            Limit the maximum number of Shot records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ShotGroupByOutput]
            A list of dictionaries representing the Shot record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Shot records by beatId values
        # and count how many records are in each group
        results = await Shot.prisma().group_by(
            ['beatId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AppActionMappingActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AppActionMapping]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AppActionMapping.prisma().query_raw(
            'SELECT * FROM AppActionMapping WHERE id = ?',
            'dahihgbeb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AppActionMapping
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AppActionMapping.prisma().query_first(
            'SELECT * FROM AppActionMapping WHERE action = ?',
            'bgheaejbcc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AppActionMappingCreateInput,
        include: Optional[types.AppActionMappingInclude] = None
    ) -> _PrismaModelT:
        """Create a new AppActionMapping record.

        Parameters
        ----------
        data
            AppActionMapping record data
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The created AppActionMapping record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AppActionMapping record from just the required fields
        appactionmapping = await AppActionMapping.prisma().create(
            data={
                # data to create a AppActionMapping record
                'action': 'bfcgifeged',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AppActionMappingCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AppActionMapping records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AppActionMapping record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AppActionMapping.prisma().create_many(
            data=[
                {
                    # data to create a AppActionMapping record
                    'action': 'jfiahhbae',
                },
                {
                    # data to create a AppActionMapping record
                    'action': 'bfbdafajcb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AppActionMappingWhereUniqueInput,
        include: Optional[types.AppActionMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AppActionMapping record.

        Parameters
        ----------
        where
            AppActionMapping filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The deleted AppActionMapping record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        appactionmapping = await AppActionMapping.prisma().delete(
            where={
                'id': 'caeghehde',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AppActionMappingWhereUniqueInput,
        include: Optional[types.AppActionMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AppActionMapping record.

        Parameters
        ----------
        where
            AppActionMapping filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The found AppActionMapping record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        appactionmapping = await AppActionMapping.prisma().find_unique(
            where={
                'id': 'caghgfbggd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AppActionMappingWhereUniqueInput,
        include: Optional[types.AppActionMappingInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AppActionMapping record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AppActionMapping filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The found AppActionMapping record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        appactionmapping = await AppActionMapping.prisma().find_unique_or_raise(
            where={
                'id': 'bbidjbbjaa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
        include: Optional[types.AppActionMappingInclude] = None,
        order: Optional[Union[types.AppActionMappingOrderByInput, List[types.AppActionMappingOrderByInput]]] = None,
        distinct: Optional[List[types.AppActionMappingScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AppActionMapping records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AppActionMapping records returned
        skip
            Ignore the first N results
        where
            AppActionMapping filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AppActionMapping model
        order
            Order the returned AppActionMapping records by any field
        distinct
            Filter AppActionMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AppActionMapping]
            The list of all AppActionMapping records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AppActionMapping records
        appactionmappings = await AppActionMapping.prisma().find_many(take=10)

        # find the first 5 AppActionMapping records ordered by the promptFile field
        appactionmappings = await AppActionMapping.prisma().find_many(
            take=5,
            order={
                'promptFile': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
        include: Optional[types.AppActionMappingInclude] = None,
        order: Optional[Union[types.AppActionMappingOrderByInput, List[types.AppActionMappingOrderByInput]]] = None,
        distinct: Optional[List[types.AppActionMappingScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AppActionMapping record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AppActionMapping filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AppActionMapping model
        order
            Order the returned AppActionMapping records by any field
        distinct
            Filter AppActionMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AppActionMapping
            The first AppActionMapping record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AppActionMapping record ordered by the createdAt field
        appactionmapping = await AppActionMapping.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
        include: Optional[types.AppActionMappingInclude] = None,
        order: Optional[Union[types.AppActionMappingOrderByInput, List[types.AppActionMappingOrderByInput]]] = None,
        distinct: Optional[List[types.AppActionMappingScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AppActionMapping record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AppActionMapping filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AppActionMapping model
        order
            Order the returned AppActionMapping records by any field
        distinct
            Filter AppActionMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AppActionMapping
            The first AppActionMapping record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AppActionMapping record ordered by the updatedAt field
        appactionmapping = await AppActionMapping.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AppActionMappingUpdateInput,
        where: types.AppActionMappingWhereUniqueInput,
        include: Optional[types.AppActionMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AppActionMapping record.

        Parameters
        ----------
        data
            AppActionMapping record data specifying what to update
        where
            AppActionMapping filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The updated AppActionMapping record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        appactionmapping = await AppActionMapping.prisma().update(
            where={
                'id': 'bfijhaejdd',
            },
            data={
                # data to update the AppActionMapping record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AppActionMappingWhereUniqueInput,
        data: types.AppActionMappingUpsertInput,
        include: Optional[types.AppActionMappingInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AppActionMapping filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AppActionMapping model

        Returns
        -------
        prisma.models.AppActionMapping
            The created or updated AppActionMapping record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        appactionmapping = await AppActionMapping.prisma().upsert(
            where={
                'id': 'bcedehfiji',
            },
            data={
                'create': {
                    'id': 'bcedehfiji',
                    'action': 'bfbdafajcb',
                },
                'update': {
                    'action': 'bfbdafajcb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AppActionMappingUpdateManyMutationInput,
        where: types.AppActionMappingWhereInput,
    ) -> int:
        """Update multiple AppActionMapping records

        Parameters
        ----------
        data
            AppActionMapping data to update the selected AppActionMapping records to
        where
            Filter to select the AppActionMapping records to update

        Returns
        -------
        int
            The total number of AppActionMapping records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AppActionMapping records
        total = await AppActionMapping.prisma().update_many(
            data={
                'id': 'bdgjicijhb'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AppActionMapping records present in the database

        Parameters
        ----------
        select
            Select the AppActionMapping fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AppActionMapping filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AppActionMappingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AppActionMapping.prisma().count()

        # results: prisma.types.AppActionMappingCountAggregateOutput
        results = await AppActionMapping.prisma().count(
            select={
                '_all': True,
                'action': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AppActionMappingCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
    ) -> types.AppActionMappingCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AppActionMappingCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AppActionMappingWhereInput] = None,
        cursor: Optional[types.AppActionMappingWhereUniqueInput] = None,
    ) -> Union[int, types.AppActionMappingCountAggregateOutput]:
        """Count the number of AppActionMapping records present in the database

        Parameters
        ----------
        select
            Select the AppActionMapping fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AppActionMapping filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AppActionMappingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AppActionMapping.prisma().count()

        # results: prisma.types.AppActionMappingCountAggregateOutput
        results = await AppActionMapping.prisma().count(
            select={
                '_all': True,
                'promptFile': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AppActionMappingCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AppActionMappingWhereInput] = None
    ) -> int:
        """Delete multiple AppActionMapping records.

        Parameters
        ----------
        where
            Optional AppActionMapping filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AppActionMapping records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AppActionMapping records
        total = await AppActionMapping.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AppActionMappingScalarFieldKeys'],
        *,
        where: Optional['types.AppActionMappingWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AppActionMappingAvgAggregateInput'] = None,
        sum: Optional['types.AppActionMappingSumAggregateInput'] = None,
        min: Optional['types.AppActionMappingMinAggregateInput'] = None,
        max: Optional['types.AppActionMappingMaxAggregateInput'] = None,
        having: Optional['types.AppActionMappingScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AppActionMappingCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AppActionMappingScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AppActionMappingScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AppActionMappingGroupByOutput']:
        """Group AppActionMapping records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AppActionMapping fields to group records by
        where
            AppActionMapping filter to select records
        take
            Limit the maximum number of AppActionMapping records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AppActionMappingGroupByOutput]
            A list of dictionaries representing the AppActionMapping record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AppActionMapping records by createdAt values
        # and count how many records are in each group
        results = await AppActionMapping.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ComfyWorkflowMappingActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ComfyWorkflowMapping]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ComfyWorkflowMapping.prisma().query_raw(
            'SELECT * FROM ComfyWorkflowMapping WHERE id = ?',
            'bghifjdeia',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ComfyWorkflowMapping.prisma().query_first(
            'SELECT * FROM ComfyWorkflowMapping WHERE action = ?',
            'eadfcbbcb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ComfyWorkflowMappingCreateInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None
    ) -> _PrismaModelT:
        """Create a new ComfyWorkflowMapping record.

        Parameters
        ----------
        data
            ComfyWorkflowMapping record data
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The created ComfyWorkflowMapping record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ComfyWorkflowMapping record from just the required fields
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().create(
            data={
                # data to create a ComfyWorkflowMapping record
                'action': 'geihgahba',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ComfyWorkflowMappingCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ComfyWorkflowMapping records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ComfyWorkflowMapping record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ComfyWorkflowMapping.prisma().create_many(
            data=[
                {
                    # data to create a ComfyWorkflowMapping record
                    'action': 'gahdcdhbj',
                },
                {
                    # data to create a ComfyWorkflowMapping record
                    'action': 'begiijahea',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ComfyWorkflowMappingWhereUniqueInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ComfyWorkflowMapping record.

        Parameters
        ----------
        where
            ComfyWorkflowMapping filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The deleted ComfyWorkflowMapping record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().delete(
            where={
                'id': 'gcjadjaaf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ComfyWorkflowMappingWhereUniqueInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ComfyWorkflowMapping record.

        Parameters
        ----------
        where
            ComfyWorkflowMapping filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The found ComfyWorkflowMapping record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().find_unique(
            where={
                'id': 'bcbebgiaic',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ComfyWorkflowMappingWhereUniqueInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ComfyWorkflowMapping record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ComfyWorkflowMapping filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The found ComfyWorkflowMapping record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().find_unique_or_raise(
            where={
                'id': 'ijigbdcbj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
        include: Optional[types.ComfyWorkflowMappingInclude] = None,
        order: Optional[Union[types.ComfyWorkflowMappingOrderByInput, List[types.ComfyWorkflowMappingOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyWorkflowMappingScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ComfyWorkflowMapping records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ComfyWorkflowMapping records returned
        skip
            Ignore the first N results
        where
            ComfyWorkflowMapping filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model
        order
            Order the returned ComfyWorkflowMapping records by any field
        distinct
            Filter ComfyWorkflowMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ComfyWorkflowMapping]
            The list of all ComfyWorkflowMapping records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ComfyWorkflowMapping records
        comfyworkflowmappings = await ComfyWorkflowMapping.prisma().find_many(take=10)

        # find the first 5 ComfyWorkflowMapping records ordered by the workflowFile field
        comfyworkflowmappings = await ComfyWorkflowMapping.prisma().find_many(
            take=5,
            order={
                'workflowFile': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
        include: Optional[types.ComfyWorkflowMappingInclude] = None,
        order: Optional[Union[types.ComfyWorkflowMappingOrderByInput, List[types.ComfyWorkflowMappingOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyWorkflowMappingScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ComfyWorkflowMapping record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ComfyWorkflowMapping filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model
        order
            Order the returned ComfyWorkflowMapping records by any field
        distinct
            Filter ComfyWorkflowMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The first ComfyWorkflowMapping record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ComfyWorkflowMapping record ordered by the resolutionJson field
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().find_first(
            skip=1,
            order={
                'resolutionJson': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
        include: Optional[types.ComfyWorkflowMappingInclude] = None,
        order: Optional[Union[types.ComfyWorkflowMappingOrderByInput, List[types.ComfyWorkflowMappingOrderByInput]]] = None,
        distinct: Optional[List[types.ComfyWorkflowMappingScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ComfyWorkflowMapping record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ComfyWorkflowMapping filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model
        order
            Order the returned ComfyWorkflowMapping records by any field
        distinct
            Filter ComfyWorkflowMapping records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The first ComfyWorkflowMapping record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ComfyWorkflowMapping record ordered by the createdAt field
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ComfyWorkflowMappingUpdateInput,
        where: types.ComfyWorkflowMappingWhereUniqueInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ComfyWorkflowMapping record.

        Parameters
        ----------
        data
            ComfyWorkflowMapping record data specifying what to update
        where
            ComfyWorkflowMapping filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The updated ComfyWorkflowMapping record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().update(
            where={
                'id': 'gfidhicai',
            },
            data={
                # data to update the ComfyWorkflowMapping record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ComfyWorkflowMappingWhereUniqueInput,
        data: types.ComfyWorkflowMappingUpsertInput,
        include: Optional[types.ComfyWorkflowMappingInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ComfyWorkflowMapping filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ComfyWorkflowMapping model

        Returns
        -------
        prisma.models.ComfyWorkflowMapping
            The created or updated ComfyWorkflowMapping record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        comfyworkflowmapping = await ComfyWorkflowMapping.prisma().upsert(
            where={
                'id': 'jfegcaafh',
            },
            data={
                'create': {
                    'id': 'jfegcaafh',
                    'action': 'begiijahea',
                },
                'update': {
                    'action': 'begiijahea',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ComfyWorkflowMappingUpdateManyMutationInput,
        where: types.ComfyWorkflowMappingWhereInput,
    ) -> int:
        """Update multiple ComfyWorkflowMapping records

        Parameters
        ----------
        data
            ComfyWorkflowMapping data to update the selected ComfyWorkflowMapping records to
        where
            Filter to select the ComfyWorkflowMapping records to update

        Returns
        -------
        int
            The total number of ComfyWorkflowMapping records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ComfyWorkflowMapping records
        total = await ComfyWorkflowMapping.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ComfyWorkflowMapping records present in the database

        Parameters
        ----------
        select
            Select the ComfyWorkflowMapping fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ComfyWorkflowMapping filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ComfyWorkflowMappingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ComfyWorkflowMapping.prisma().count()

        # results: prisma.types.ComfyWorkflowMappingCountAggregateOutput
        results = await ComfyWorkflowMapping.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ComfyWorkflowMappingCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
    ) -> types.ComfyWorkflowMappingCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ComfyWorkflowMappingCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None,
        cursor: Optional[types.ComfyWorkflowMappingWhereUniqueInput] = None,
    ) -> Union[int, types.ComfyWorkflowMappingCountAggregateOutput]:
        """Count the number of ComfyWorkflowMapping records present in the database

        Parameters
        ----------
        select
            Select the ComfyWorkflowMapping fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ComfyWorkflowMapping filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ComfyWorkflowMappingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ComfyWorkflowMapping.prisma().count()

        # results: prisma.types.ComfyWorkflowMappingCountAggregateOutput
        results = await ComfyWorkflowMapping.prisma().count(
            select={
                '_all': True,
                'action': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ComfyWorkflowMappingCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ComfyWorkflowMappingWhereInput] = None
    ) -> int:
        """Delete multiple ComfyWorkflowMapping records.

        Parameters
        ----------
        where
            Optional ComfyWorkflowMapping filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ComfyWorkflowMapping records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ComfyWorkflowMapping records
        total = await ComfyWorkflowMapping.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ComfyWorkflowMappingScalarFieldKeys'],
        *,
        where: Optional['types.ComfyWorkflowMappingWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ComfyWorkflowMappingAvgAggregateInput'] = None,
        sum: Optional['types.ComfyWorkflowMappingSumAggregateInput'] = None,
        min: Optional['types.ComfyWorkflowMappingMinAggregateInput'] = None,
        max: Optional['types.ComfyWorkflowMappingMaxAggregateInput'] = None,
        having: Optional['types.ComfyWorkflowMappingScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ComfyWorkflowMappingCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ComfyWorkflowMappingScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ComfyWorkflowMappingScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ComfyWorkflowMappingGroupByOutput']:
        """Group ComfyWorkflowMapping records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ComfyWorkflowMapping fields to group records by
        where
            ComfyWorkflowMapping filter to select records
        take
            Limit the maximum number of ComfyWorkflowMapping records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ComfyWorkflowMappingGroupByOutput]
            A list of dictionaries representing the ComfyWorkflowMapping record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ComfyWorkflowMapping records by workflowFile values
        # and count how many records are in each group
        results = await ComfyWorkflowMapping.prisma().group_by(
            ['workflowFile'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ScriptActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Script]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Script.prisma().query_raw(
            'SELECT * FROM Script WHERE id = ?',
            'bcbeiajjfa',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Script
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Script.prisma().query_first(
            'SELECT * FROM Script WHERE projectId = ?',
            'baehicaajf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ScriptCreateInput,
        include: Optional[types.ScriptInclude] = None
    ) -> _PrismaModelT:
        """Create a new Script record.

        Parameters
        ----------
        data
            Script record data
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The created Script record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Script record from just the required fields
        script = await Script.prisma().create(
            data={
                # data to create a Script record
                'projectId': 'bdachdeiga',
                'content': 'ijdafccef',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ScriptCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Script records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Script record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Script.prisma().create_many(
            data=[
                {
                    # data to create a Script record
                    'projectId': 'ciaaiddag',
                    'content': 'fejggijff',
                },
                {
                    # data to create a Script record
                    'projectId': 'hghjaaai',
                    'content': 'cajicjjdef',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ScriptWhereUniqueInput,
        include: Optional[types.ScriptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Script record.

        Parameters
        ----------
        where
            Script filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The deleted Script record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        script = await Script.prisma().delete(
            where={
                'id': 'cefjaadec',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ScriptWhereUniqueInput,
        include: Optional[types.ScriptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Script record.

        Parameters
        ----------
        where
            Script filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The found Script record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        script = await Script.prisma().find_unique(
            where={
                'id': 'ibbigdigd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ScriptWhereUniqueInput,
        include: Optional[types.ScriptInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Script record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Script filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The found Script record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        script = await Script.prisma().find_unique_or_raise(
            where={
                'id': 'bdiiiabbii',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
        include: Optional[types.ScriptInclude] = None,
        order: Optional[Union[types.ScriptOrderByInput, List[types.ScriptOrderByInput]]] = None,
        distinct: Optional[List[types.ScriptScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Script records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Script records returned
        skip
            Ignore the first N results
        where
            Script filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Script model
        order
            Order the returned Script records by any field
        distinct
            Filter Script records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Script]
            The list of all Script records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Script records
        scripts = await Script.prisma().find_many(take=10)

        # find the first 5 Script records ordered by the content field
        scripts = await Script.prisma().find_many(
            take=5,
            order={
                'content': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
        include: Optional[types.ScriptInclude] = None,
        order: Optional[Union[types.ScriptOrderByInput, List[types.ScriptOrderByInput]]] = None,
        distinct: Optional[List[types.ScriptScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Script record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Script filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Script model
        order
            Order the returned Script records by any field
        distinct
            Filter Script records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Script
            The first Script record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Script record ordered by the createdAt field
        script = await Script.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
        include: Optional[types.ScriptInclude] = None,
        order: Optional[Union[types.ScriptOrderByInput, List[types.ScriptOrderByInput]]] = None,
        distinct: Optional[List[types.ScriptScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Script record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Script filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Script model
        order
            Order the returned Script records by any field
        distinct
            Filter Script records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Script
            The first Script record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Script record ordered by the updatedAt field
        script = await Script.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ScriptUpdateInput,
        where: types.ScriptWhereUniqueInput,
        include: Optional[types.ScriptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Script record.

        Parameters
        ----------
        data
            Script record data specifying what to update
        where
            Script filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The updated Script record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        script = await Script.prisma().update(
            where={
                'id': 'hfcfhhadh',
            },
            data={
                # data to update the Script record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ScriptWhereUniqueInput,
        data: types.ScriptUpsertInput,
        include: Optional[types.ScriptInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Script filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Script model

        Returns
        -------
        prisma.models.Script
            The created or updated Script record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        script = await Script.prisma().upsert(
            where={
                'id': 'bbihggdcji',
            },
            data={
                'create': {
                    'id': 'bbihggdcji',
                    'projectId': 'hghjaaai',
                    'content': 'cajicjjdef',
                },
                'update': {
                    'projectId': 'hghjaaai',
                    'content': 'cajicjjdef',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ScriptUpdateManyMutationInput,
        where: types.ScriptWhereInput,
    ) -> int:
        """Update multiple Script records

        Parameters
        ----------
        data
            Script data to update the selected Script records to
        where
            Filter to select the Script records to update

        Returns
        -------
        int
            The total number of Script records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Script records
        total = await Script.prisma().update_many(
            data={
                'id': 'hgjgibdgd'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Script records present in the database

        Parameters
        ----------
        select
            Select the Script fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Script filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScriptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Script.prisma().count()

        # results: prisma.types.ScriptCountAggregateOutput
        results = await Script.prisma().count(
            select={
                '_all': True,
                'projectId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ScriptCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
    ) -> types.ScriptCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ScriptCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScriptWhereInput] = None,
        cursor: Optional[types.ScriptWhereUniqueInput] = None,
    ) -> Union[int, types.ScriptCountAggregateOutput]:
        """Count the number of Script records present in the database

        Parameters
        ----------
        select
            Select the Script fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Script filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScriptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Script.prisma().count()

        # results: prisma.types.ScriptCountAggregateOutput
        results = await Script.prisma().count(
            select={
                '_all': True,
                'content': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ScriptCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ScriptWhereInput] = None
    ) -> int:
        """Delete multiple Script records.

        Parameters
        ----------
        where
            Optional Script filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Script records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Script records
        total = await Script.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ScriptScalarFieldKeys'],
        *,
        where: Optional['types.ScriptWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ScriptAvgAggregateInput'] = None,
        sum: Optional['types.ScriptSumAggregateInput'] = None,
        min: Optional['types.ScriptMinAggregateInput'] = None,
        max: Optional['types.ScriptMaxAggregateInput'] = None,
        having: Optional['types.ScriptScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ScriptCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ScriptScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ScriptScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ScriptGroupByOutput']:
        """Group Script records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Script fields to group records by
        where
            Script filter to select records
        take
            Limit the maximum number of Script records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ScriptGroupByOutput]
            A list of dictionaries representing the Script record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Script records by createdAt values
        # and count how many records are in each group
        results = await Script.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FinalVideoActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FinalVideo]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await FinalVideo.prisma().query_raw(
            'SELECT * FROM FinalVideo WHERE id = ?',
            'bcbecjfice',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FinalVideo
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await FinalVideo.prisma().query_first(
            'SELECT * FROM FinalVideo WHERE projectId = ?',
            'bacbebhjjd',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.FinalVideoCreateInput,
        include: Optional[types.FinalVideoInclude] = None
    ) -> _PrismaModelT:
        """Create a new FinalVideo record.

        Parameters
        ----------
        data
            FinalVideo record data
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The created FinalVideo record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FinalVideo record from just the required fields
        finalvideo = await FinalVideo.prisma().create(
            data={
                # data to create a FinalVideo record
                'projectId': 'dfbfaddhe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.FinalVideoCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FinalVideo records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FinalVideo record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await FinalVideo.prisma().create_many(
            data=[
                {
                    # data to create a FinalVideo record
                    'projectId': 'bdcbbieibf',
                },
                {
                    # data to create a FinalVideo record
                    'projectId': 'dgjhdcggi',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.FinalVideoWhereUniqueInput,
        include: Optional[types.FinalVideoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FinalVideo record.

        Parameters
        ----------
        where
            FinalVideo filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The deleted FinalVideo record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        finalvideo = await FinalVideo.prisma().delete(
            where={
                'id': 'bbjbcdfabd',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.FinalVideoWhereUniqueInput,
        include: Optional[types.FinalVideoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FinalVideo record.

        Parameters
        ----------
        where
            FinalVideo filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The found FinalVideo record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        finalvideo = await FinalVideo.prisma().find_unique(
            where={
                'id': 'gchfgbcec',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.FinalVideoWhereUniqueInput,
        include: Optional[types.FinalVideoInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FinalVideo record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FinalVideo filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The found FinalVideo record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        finalvideo = await FinalVideo.prisma().find_unique_or_raise(
            where={
                'id': 'bihcjfcjah',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
        include: Optional[types.FinalVideoInclude] = None,
        order: Optional[Union[types.FinalVideoOrderByInput, List[types.FinalVideoOrderByInput]]] = None,
        distinct: Optional[List[types.FinalVideoScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FinalVideo records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FinalVideo records returned
        skip
            Ignore the first N results
        where
            FinalVideo filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinalVideo model
        order
            Order the returned FinalVideo records by any field
        distinct
            Filter FinalVideo records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FinalVideo]
            The list of all FinalVideo records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FinalVideo records
        finalvideos = await FinalVideo.prisma().find_many(take=10)

        # find the first 5 FinalVideo records ordered by the totalDuration field
        finalvideos = await FinalVideo.prisma().find_many(
            take=5,
            order={
                'totalDuration': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
        include: Optional[types.FinalVideoInclude] = None,
        order: Optional[Union[types.FinalVideoOrderByInput, List[types.FinalVideoOrderByInput]]] = None,
        distinct: Optional[List[types.FinalVideoScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FinalVideo record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinalVideo filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinalVideo model
        order
            Order the returned FinalVideo records by any field
        distinct
            Filter FinalVideo records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinalVideo
            The first FinalVideo record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinalVideo record ordered by the exportPath field
        finalvideo = await FinalVideo.prisma().find_first(
            skip=1,
            order={
                'exportPath': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
        include: Optional[types.FinalVideoInclude] = None,
        order: Optional[Union[types.FinalVideoOrderByInput, List[types.FinalVideoOrderByInput]]] = None,
        distinct: Optional[List[types.FinalVideoScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FinalVideo record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FinalVideo filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FinalVideo model
        order
            Order the returned FinalVideo records by any field
        distinct
            Filter FinalVideo records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FinalVideo
            The first FinalVideo record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FinalVideo record ordered by the thumbnailUrl field
        finalvideo = await FinalVideo.prisma().find_first_or_raise(
            skip=1,
            order={
                'thumbnailUrl': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.FinalVideoUpdateInput,
        where: types.FinalVideoWhereUniqueInput,
        include: Optional[types.FinalVideoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FinalVideo record.

        Parameters
        ----------
        data
            FinalVideo record data specifying what to update
        where
            FinalVideo filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The updated FinalVideo record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        finalvideo = await FinalVideo.prisma().update(
            where={
                'id': 'bhjdcicaii',
            },
            data={
                # data to update the FinalVideo record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.FinalVideoWhereUniqueInput,
        data: types.FinalVideoUpsertInput,
        include: Optional[types.FinalVideoInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FinalVideo filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FinalVideo model

        Returns
        -------
        prisma.models.FinalVideo
            The created or updated FinalVideo record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        finalvideo = await FinalVideo.prisma().upsert(
            where={
                'id': 'bibedjhcej',
            },
            data={
                'create': {
                    'id': 'bibedjhcej',
                    'projectId': 'dgjhdcggi',
                },
                'update': {
                    'projectId': 'dgjhdcggi',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.FinalVideoUpdateManyMutationInput,
        where: types.FinalVideoWhereInput,
    ) -> int:
        """Update multiple FinalVideo records

        Parameters
        ----------
        data
            FinalVideo data to update the selected FinalVideo records to
        where
            Filter to select the FinalVideo records to update

        Returns
        -------
        int
            The total number of FinalVideo records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FinalVideo records
        total = await FinalVideo.prisma().update_many(
            data={
                'id': 'bjcdajabfa'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FinalVideo records present in the database

        Parameters
        ----------
        select
            Select the FinalVideo fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinalVideo filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinalVideoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FinalVideo.prisma().count()

        # results: prisma.types.FinalVideoCountAggregateOutput
        results = await FinalVideo.prisma().count(
            select={
                '_all': True,
                'projectId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.FinalVideoCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
    ) -> types.FinalVideoCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.FinalVideoCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FinalVideoWhereInput] = None,
        cursor: Optional[types.FinalVideoWhereUniqueInput] = None,
    ) -> Union[int, types.FinalVideoCountAggregateOutput]:
        """Count the number of FinalVideo records present in the database

        Parameters
        ----------
        select
            Select the FinalVideo fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FinalVideo filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FinalVideoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FinalVideo.prisma().count()

        # results: prisma.types.FinalVideoCountAggregateOutput
        results = await FinalVideo.prisma().count(
            select={
                '_all': True,
                'totalDuration': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FinalVideoCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.FinalVideoWhereInput] = None
    ) -> int:
        """Delete multiple FinalVideo records.

        Parameters
        ----------
        where
            Optional FinalVideo filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FinalVideo records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FinalVideo records
        total = await FinalVideo.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.FinalVideoScalarFieldKeys'],
        *,
        where: Optional['types.FinalVideoWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FinalVideoAvgAggregateInput'] = None,
        sum: Optional['types.FinalVideoSumAggregateInput'] = None,
        min: Optional['types.FinalVideoMinAggregateInput'] = None,
        max: Optional['types.FinalVideoMaxAggregateInput'] = None,
        having: Optional['types.FinalVideoScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FinalVideoCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FinalVideoScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FinalVideoScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FinalVideoGroupByOutput']:
        """Group FinalVideo records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FinalVideo fields to group records by
        where
            FinalVideo filter to select records
        take
            Limit the maximum number of FinalVideo records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FinalVideoGroupByOutput]
            A list of dictionaries representing the FinalVideo record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FinalVideo records by exportPath values
        # and count how many records are in each group
        results = await FinalVideo.prisma().group_by(
            ['exportPath'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AudioAssetActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AudioAsset]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AudioAsset.prisma().query_raw(
            'SELECT * FROM AudioAsset WHERE id = ?',
            'bchhceeeff',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AudioAsset
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AudioAsset.prisma().query_first(
            'SELECT * FROM AudioAsset WHERE projectId = ?',
            'bbgaifhdaa',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AudioAssetCreateInput,
        include: Optional[types.AudioAssetInclude] = None
    ) -> _PrismaModelT:
        """Create a new AudioAsset record.

        Parameters
        ----------
        data
            AudioAsset record data
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The created AudioAsset record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AudioAsset record from just the required fields
        audioasset = await AudioAsset.prisma().create(
            data={
                # data to create a AudioAsset record
                'projectId': 'dgbcdaegb',
                'name': 'beagfbbjig',
                'audioPath': 'beicihhijb',
                'audioType': 'fgggcdcjg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AudioAssetCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AudioAsset records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AudioAsset record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AudioAsset.prisma().create_many(
            data=[
                {
                    # data to create a AudioAsset record
                    'projectId': 'ccjbbjigf',
                    'name': 'bhfaabbaha',
                    'audioPath': 'ebajedhhf',
                    'audioType': 'jajacedge',
                },
                {
                    # data to create a AudioAsset record
                    'projectId': 'hffgbabgf',
                    'name': 'biacbiieja',
                    'audioPath': 'cjejbgbff',
                    'audioType': 'fgeahddae',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AudioAssetWhereUniqueInput,
        include: Optional[types.AudioAssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AudioAsset record.

        Parameters
        ----------
        where
            AudioAsset filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The deleted AudioAsset record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audioasset = await AudioAsset.prisma().delete(
            where={
                'id': 'diageigcf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AudioAssetWhereUniqueInput,
        include: Optional[types.AudioAssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AudioAsset record.

        Parameters
        ----------
        where
            AudioAsset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The found AudioAsset record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audioasset = await AudioAsset.prisma().find_unique(
            where={
                'id': 'badagbgeha',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AudioAssetWhereUniqueInput,
        include: Optional[types.AudioAssetInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AudioAsset record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AudioAsset filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The found AudioAsset record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audioasset = await AudioAsset.prisma().find_unique_or_raise(
            where={
                'id': 'ibgebbjch',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
        include: Optional[types.AudioAssetInclude] = None,
        order: Optional[Union[types.AudioAssetOrderByInput, List[types.AudioAssetOrderByInput]]] = None,
        distinct: Optional[List[types.AudioAssetScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AudioAsset records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AudioAsset records returned
        skip
            Ignore the first N results
        where
            AudioAsset filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudioAsset model
        order
            Order the returned AudioAsset records by any field
        distinct
            Filter AudioAsset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AudioAsset]
            The list of all AudioAsset records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AudioAsset records
        audioassets = await AudioAsset.prisma().find_many(take=10)

        # find the first 5 AudioAsset records ordered by the name field
        audioassets = await AudioAsset.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
        include: Optional[types.AudioAssetInclude] = None,
        order: Optional[Union[types.AudioAssetOrderByInput, List[types.AudioAssetOrderByInput]]] = None,
        distinct: Optional[List[types.AudioAssetScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AudioAsset record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudioAsset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudioAsset model
        order
            Order the returned AudioAsset records by any field
        distinct
            Filter AudioAsset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudioAsset
            The first AudioAsset record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudioAsset record ordered by the audioPath field
        audioasset = await AudioAsset.prisma().find_first(
            skip=1,
            order={
                'audioPath': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
        include: Optional[types.AudioAssetInclude] = None,
        order: Optional[Union[types.AudioAssetOrderByInput, List[types.AudioAssetOrderByInput]]] = None,
        distinct: Optional[List[types.AudioAssetScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AudioAsset record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudioAsset filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudioAsset model
        order
            Order the returned AudioAsset records by any field
        distinct
            Filter AudioAsset records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudioAsset
            The first AudioAsset record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudioAsset record ordered by the audioType field
        audioasset = await AudioAsset.prisma().find_first_or_raise(
            skip=1,
            order={
                'audioType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AudioAssetUpdateInput,
        where: types.AudioAssetWhereUniqueInput,
        include: Optional[types.AudioAssetInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AudioAsset record.

        Parameters
        ----------
        data
            AudioAsset record data specifying what to update
        where
            AudioAsset filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The updated AudioAsset record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        audioasset = await AudioAsset.prisma().update(
            where={
                'id': 'baieajjiee',
            },
            data={
                # data to update the AudioAsset record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AudioAssetWhereUniqueInput,
        data: types.AudioAssetUpsertInput,
        include: Optional[types.AudioAssetInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AudioAsset filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AudioAsset model

        Returns
        -------
        prisma.models.AudioAsset
            The created or updated AudioAsset record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audioasset = await AudioAsset.prisma().upsert(
            where={
                'id': 'bahjhaccfd',
            },
            data={
                'create': {
                    'id': 'bahjhaccfd',
                    'projectId': 'hffgbabgf',
                    'name': 'biacbiieja',
                    'audioPath': 'cjejbgbff',
                    'audioType': 'fgeahddae',
                },
                'update': {
                    'projectId': 'hffgbabgf',
                    'name': 'biacbiieja',
                    'audioPath': 'cjejbgbff',
                    'audioType': 'fgeahddae',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AudioAssetUpdateManyMutationInput,
        where: types.AudioAssetWhereInput,
    ) -> int:
        """Update multiple AudioAsset records

        Parameters
        ----------
        data
            AudioAsset data to update the selected AudioAsset records to
        where
            Filter to select the AudioAsset records to update

        Returns
        -------
        int
            The total number of AudioAsset records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AudioAsset records
        total = await AudioAsset.prisma().update_many(
            data={
                'transcript': 'hffhfabhi'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AudioAsset records present in the database

        Parameters
        ----------
        select
            Select the AudioAsset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudioAsset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudioAssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudioAsset.prisma().count()

        # results: prisma.types.AudioAssetCountAggregateOutput
        results = await AudioAsset.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AudioAssetCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
    ) -> types.AudioAssetCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AudioAssetCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudioAssetWhereInput] = None,
        cursor: Optional[types.AudioAssetWhereUniqueInput] = None,
    ) -> Union[int, types.AudioAssetCountAggregateOutput]:
        """Count the number of AudioAsset records present in the database

        Parameters
        ----------
        select
            Select the AudioAsset fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudioAsset filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudioAssetCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudioAsset.prisma().count()

        # results: prisma.types.AudioAssetCountAggregateOutput
        results = await AudioAsset.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AudioAssetCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AudioAssetWhereInput] = None
    ) -> int:
        """Delete multiple AudioAsset records.

        Parameters
        ----------
        where
            Optional AudioAsset filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AudioAsset records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AudioAsset records
        total = await AudioAsset.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AudioAssetScalarFieldKeys'],
        *,
        where: Optional['types.AudioAssetWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AudioAssetAvgAggregateInput'] = None,
        sum: Optional['types.AudioAssetSumAggregateInput'] = None,
        min: Optional['types.AudioAssetMinAggregateInput'] = None,
        max: Optional['types.AudioAssetMaxAggregateInput'] = None,
        having: Optional['types.AudioAssetScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AudioAssetCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AudioAssetScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AudioAssetScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AudioAssetGroupByOutput']:
        """Group AudioAsset records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AudioAsset fields to group records by
        where
            AudioAsset filter to select records
        take
            Limit the maximum number of AudioAsset records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AudioAssetGroupByOutput]
            A list of dictionaries representing the AudioAsset record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AudioAsset records by id values
        # and count how many records are in each group
        results = await AudioAsset.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ShotListActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ShotList]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ShotList.prisma().query_raw(
            'SELECT * FROM ShotList WHERE id = ?',
            'bbcigiadhb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ShotList
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ShotList.prisma().query_first(
            'SELECT * FROM ShotList WHERE projectId = ?',
            'cfjagbbae',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ShotListCreateInput,
        include: Optional[types.ShotListInclude] = None
    ) -> _PrismaModelT:
        """Create a new ShotList record.

        Parameters
        ----------
        data
            ShotList record data
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The created ShotList record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ShotList record from just the required fields
        shotlist = await ShotList.prisma().create(
            data={
                # data to create a ShotList record
                'projectId': 'bbbfhdidef',
                'shot': 1303781742,
                'frames': 1573908495,
                'duration': 1195899036.73902,
                'camera': 'bhcccbeaba',
                'action': 'bcgjbdgjdj',
                'dialogue': 'fhdbhifae',
                'note': 'beeacgfcej',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ShotListCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ShotList records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ShotList record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ShotList.prisma().create_many(
            data=[
                {
                    # data to create a ShotList record
                    'projectId': 'bbifhdiicc',
                    'shot': 1694224903,
                    'frames': 1906360116,
                    'duration': 1911255389.14626,
                    'camera': 'bhbjceagbb',
                    'action': 'bjeifffjdg',
                    'dialogue': 'bdidcfdfjd',
                    'note': 'dfeggejja',
                },
                {
                    # data to create a ShotList record
                    'projectId': 'gehbgghbj',
                    'shot': 357089484,
                    'frames': 612396821,
                    'duration': 1808130602.115174,
                    'camera': 'hiagajie',
                    'action': 'eeejidbif',
                    'dialogue': 'efgbahec',
                    'note': 'hgjaiebfb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ShotListWhereUniqueInput,
        include: Optional[types.ShotListInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ShotList record.

        Parameters
        ----------
        where
            ShotList filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The deleted ShotList record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shotlist = await ShotList.prisma().delete(
            where={
                'id': 'bddefjjabc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ShotListWhereUniqueInput,
        include: Optional[types.ShotListInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ShotList record.

        Parameters
        ----------
        where
            ShotList filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The found ShotList record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shotlist = await ShotList.prisma().find_unique(
            where={
                'id': 'bbbghgbadh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ShotListWhereUniqueInput,
        include: Optional[types.ShotListInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ShotList record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ShotList filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The found ShotList record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shotlist = await ShotList.prisma().find_unique_or_raise(
            where={
                'id': 'bbhcgagaic',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
        include: Optional[types.ShotListInclude] = None,
        order: Optional[Union[types.ShotListOrderByInput, List[types.ShotListOrderByInput]]] = None,
        distinct: Optional[List[types.ShotListScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ShotList records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ShotList records returned
        skip
            Ignore the first N results
        where
            ShotList filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ShotList model
        order
            Order the returned ShotList records by any field
        distinct
            Filter ShotList records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ShotList]
            The list of all ShotList records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ShotList records
        shotlists = await ShotList.prisma().find_many(take=10)

        # find the first 5 ShotList records ordered by the shot field
        shotlists = await ShotList.prisma().find_many(
            take=5,
            order={
                'shot': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
        include: Optional[types.ShotListInclude] = None,
        order: Optional[Union[types.ShotListOrderByInput, List[types.ShotListOrderByInput]]] = None,
        distinct: Optional[List[types.ShotListScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ShotList record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ShotList filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ShotList model
        order
            Order the returned ShotList records by any field
        distinct
            Filter ShotList records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ShotList
            The first ShotList record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ShotList record ordered by the scene field
        shotlist = await ShotList.prisma().find_first(
            skip=1,
            order={
                'scene': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
        include: Optional[types.ShotListInclude] = None,
        order: Optional[Union[types.ShotListOrderByInput, List[types.ShotListOrderByInput]]] = None,
        distinct: Optional[List[types.ShotListScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ShotList record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ShotList filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ShotList model
        order
            Order the returned ShotList records by any field
        distinct
            Filter ShotList records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ShotList
            The first ShotList record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ShotList record ordered by the beats field
        shotlist = await ShotList.prisma().find_first_or_raise(
            skip=1,
            order={
                'beats': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ShotListUpdateInput,
        where: types.ShotListWhereUniqueInput,
        include: Optional[types.ShotListInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ShotList record.

        Parameters
        ----------
        data
            ShotList record data specifying what to update
        where
            ShotList filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The updated ShotList record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        shotlist = await ShotList.prisma().update(
            where={
                'id': 'ddaabegbb',
            },
            data={
                # data to update the ShotList record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ShotListWhereUniqueInput,
        data: types.ShotListUpsertInput,
        include: Optional[types.ShotListInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ShotList filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ShotList model

        Returns
        -------
        prisma.models.ShotList
            The created or updated ShotList record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        shotlist = await ShotList.prisma().upsert(
            where={
                'id': 'bhgibfgbbc',
            },
            data={
                'create': {
                    'id': 'bhgibfgbbc',
                    'projectId': 'gehbgghbj',
                    'shot': 357089484,
                    'frames': 612396821,
                    'duration': 1808130602.115174,
                    'camera': 'hiagajie',
                    'action': 'eeejidbif',
                    'dialogue': 'efgbahec',
                    'note': 'hgjaiebfb',
                },
                'update': {
                    'projectId': 'gehbgghbj',
                    'shot': 357089484,
                    'frames': 612396821,
                    'duration': 1808130602.115174,
                    'camera': 'hiagajie',
                    'action': 'eeejidbif',
                    'dialogue': 'efgbahec',
                    'note': 'hgjaiebfb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ShotListUpdateManyMutationInput,
        where: types.ShotListWhereInput,
    ) -> int:
        """Update multiple ShotList records

        Parameters
        ----------
        data
            ShotList data to update the selected ShotList records to
        where
            Filter to select the ShotList records to update

        Returns
        -------
        int
            The total number of ShotList records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ShotList records
        total = await ShotList.prisma().update_many(
            data={
                'loc': 'hbgcihef'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ShotList records present in the database

        Parameters
        ----------
        select
            Select the ShotList fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ShotList filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ShotListCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ShotList.prisma().count()

        # results: prisma.types.ShotListCountAggregateOutput
        results = await ShotList.prisma().count(
            select={
                '_all': True,
                'subs': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ShotListCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
    ) -> types.ShotListCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ShotListCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ShotListWhereInput] = None,
        cursor: Optional[types.ShotListWhereUniqueInput] = None,
    ) -> Union[int, types.ShotListCountAggregateOutput]:
        """Count the number of ShotList records present in the database

        Parameters
        ----------
        select
            Select the ShotList fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ShotList filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ShotListCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ShotList.prisma().count()

        # results: prisma.types.ShotListCountAggregateOutput
        results = await ShotList.prisma().count(
            select={
                '_all': True,
                'frames': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ShotListCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ShotListWhereInput] = None
    ) -> int:
        """Delete multiple ShotList records.

        Parameters
        ----------
        where
            Optional ShotList filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ShotList records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ShotList records
        total = await ShotList.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ShotListScalarFieldKeys'],
        *,
        where: Optional['types.ShotListWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ShotListAvgAggregateInput'] = None,
        sum: Optional['types.ShotListSumAggregateInput'] = None,
        min: Optional['types.ShotListMinAggregateInput'] = None,
        max: Optional['types.ShotListMaxAggregateInput'] = None,
        having: Optional['types.ShotListScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ShotListCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ShotListScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ShotListScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ShotListGroupByOutput']:
        """Group ShotList records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ShotList fields to group records by
        where
            ShotList filter to select records
        take
            Limit the maximum number of ShotList records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ShotListGroupByOutput]
            A list of dictionaries representing the ShotList record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ShotList records by duration values
        # and count how many records are in each group
        results = await ShotList.prisma().group_by(
            ['duration'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models